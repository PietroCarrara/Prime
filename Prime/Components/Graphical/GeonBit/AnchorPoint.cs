using System;

namespace Prime.UI
{
	// A enum replicating GeonBit's anchor,
	// so the engine users do not have to
	// refer to GeonBit's
	public enum AnchorPoint
	{
		Center,
		TopLeft,
		TopRight,
		TopCenter,
		BottomLeft,
		BottomRight,
		BottomCenter,
		CenterLeft,
		CenterRight,
		Auto,
		AutoInline,
		AutoCenter
	}
}

